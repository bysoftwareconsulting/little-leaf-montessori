
  # Montessori website design (Copy)

  This is a code bundle for Montessori website design (Copy). The original project is available at https://www.figma.com/design/exO0PyDY0WpYogtxmCSeEi/Montessori-website-design--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  